START
  ↓
┌─────────────────┐
│ npm install     │
│ express bcryptjs│
│ jsonwebtoken    │
│ mongoose cors   │
└─────────┬───────┘
          ↓
┌─────────────────┐
│ npm start       │
│ localhost:5000  │
└─────────┬───────┘
          ↓
┌─────────────────┐
│ 1. MIDDLEWARE  │
│ cors() bodyParser│
│ app.use()       │
└─────────┬───────┘
          ↓
┌─────────────────┐
│ 2. DB CONNECT  │
│ MongoDB Atlas  │
│ or localhost   │
└─────────┬───────┘
          ↓
┌─────────────────┐
│ 3. User Model  │
│ (email, password│
│  hashed)        │
└─────────┬───────┘
          ↓
┌─────────────────┐
│ 4. ROUTES FILE │
│ /routes/auth.js │
└─────────┬───────┘
          ↓
    ┌──────┴──────┐
    │             │
 ┌──▼──┐       ┌──▼──┐
 │POST │       │POST │
 │/login│       │/signup│
 └────┼──┐    └────┼──┘
      ↓   │         ↓
 ┌────┴──┐│    ┌────┴──┘
 │ 5. REQ ││    │ 6. Hash │
 │Body:   ││    │Password │
 │{email, ││    │bcryptjs │
 │pass}   ││    └───────┘
 └────┼──┘│
      ↓   │
 ┌────┴──┐│
 │ 7. Find││
 │User by││ ← User.findOne({email})
 │Email  ││
 └────┼──┘│
      ↓   │
 ┌────┴──┐│
 │ 8. Check││
 │Password││ ← bcrypt.compare()
 └────┼──┘│
      ↓   │
    ┌────▼────┐│
    │9. JWT   ││
    │Token    ││ ← jwt.sign({id})
    │Generate ││
    └────┼──┘│
         ↓   │
 ┌────┴──┐  │
 │10. SEND│  │ ← res.json({token})
 │RESPONSE│  │
 └────┼──┘  │
      ↓     │
 ┌────┴──┐  │
 │FRONTEND│  │
 │Receives│  │
 │Token   │  │
 └───────┘  │
            │
       ┌────▼────┐
       │ERROR PATH│
       │Wrong Email│
       │or Password│
       └────┼──┘
            ↓
       ┌────┼──┐
       │res.status│
       │(401).json│
       └────┘



backend/
├── package.json (express, bcryptjs, jwt, mongoose)
├── server.js
├── models/
│   └── User.js
├── routes/
│   └── auth.js
├── middleware/
│   └── auth.js (verifyToken)
└── config/
    └── db.js


Frontend POST /api/auth/login
     ↓
Backend auth.js route
     ↓
JWT Token → localStorage
     ↓
Protected Routes (Categories/Courses)
     ↓
Authorization: Bearer <token>


express - Server & Routes

bcryptjs - Password Hashing

jsonwebtoken - JWT Tokens

mongoose - MongoDB

cors - Frontend Connection

dotenv - Environment Variables

Complete Flow: npm start → React Login → JWT Token → Protected Dashboard
